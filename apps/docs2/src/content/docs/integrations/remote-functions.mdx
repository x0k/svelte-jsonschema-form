---
title: Remote functions
type: package
packageCodeName: "sveltekit"
sidebar:
  order: 200
  badge:
    text: exp
    variant: caution
---

import { Code, Tabs, TabItem } from "@astrojs/starlight/components";
import { sveltekitPackage } from "meta";

import { getPackageDependencies } from "@/shared";
import Npm from "@/components/npm.astro";
import Deps from "@/components/peer-dependencies.astro";

import modelCode from "examples/sveltekit-starter/src/lib/post.ts?raw";
import clientCode from "examples/sveltekit-starter/src/routes/remote-functions/+page.svelte?raw";
import clientEnhanceCode from "examples/sveltekit-starter/src/routes/remote-functions-enhance/+page.svelte?raw";
import serverCode from "examples/sveltekit-starter/src/routes/remote-functions/data.remote?raw";

:::caution

Remote functions integration is experimental

:::

## Installation

<Npm pkg={getPackageDependencies(sveltekitPackage)} />
{/* Svelte `hydratable` API: https://github.com/sveltejs/svelte/pull/17154 */}
{/* SvelteKit programmatic submission API: https://github.com/sveltejs/kit/pull/15657 */}
<Deps
  pkg={sveltekitPackage}
  versionOverrides={{ "@sveltejs/kit": "2.61.0", svelte: "5.44.0" }}
/>

## Example

:::note

For correct operation, you must use ID Builder from the `@sjsf/sveltekit/rf`.

:::

{/* TODO: Fix `$lib/remote-form-defaults` */}

<Tabs>
  <TabItem label="+page.svelte">
    <Code code={clientCode} lang="svelte" />
  </TabItem>
  <TabItem label="+page.svelte (enhance)">
    <Code code={clientEnhanceCode} lang="svelte" />
  </TabItem>
  <TabItem label="data.remote.ts">
    <Code code={serverCode} lang="typescript" />
  </TabItem>
  <TabItem label="lib/post.ts">
    <Code code={modelCode} lang="typescript" />
  </TabItem>
</Tabs>

## Known issues

- `experimental.async` does not apply to dependency code -
  [issue](https://github.com/sveltejs/kit/issues/14788).

  Add this to your `vite.config.js`

  ```js
  optimizeDeps: {
    exclude: ['@sjsf/form', '@sjsf/sveltekit/rf/client'],
  }
  ```

- The `form.value` type on the client side is `unknown` because the `RemoteForm`
  type does not include information about the validator's output type.
  Specify the type via `connect` generic: `createForm(await connect<FormValue>(...))`.
