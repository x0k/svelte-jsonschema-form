---
title: Zod
type: package
packageCodeName: "zod4"
sidebar:
  order: 1
---

import { externalValidatorPackage } from "meta";

import OpenInSandbox from "@/components/open-in-sandbox.svelte";

import { getPackageDependencies } from "@/shared";
import Npm from "@/components/npm.astro";
import Deps from "@/components/peer-dependencies.astro";
import Demo from "@/components/demo.svelte";

export const pkg = externalValidatorPackage("zod4");

You may want to use Zod validator because:

- You can use Zod schema as a source of truth for the form value type (`z.infer`).
- This is an easy way to add custom error messages

:::note

Schemas with [unrepresentable](https://zod.dev/json-schema#unrepresentable) types (`z.date()`, `z.bigint()`, `z.map()`, etc.) are silently converted to `{}` (unknown). Use custom field components via UI schema to render appropriate widgets for these types.

:::

## Installation

<Npm pkg={getPackageDependencies(pkg)} />

<Deps pkg={pkg} />

## Example

:::tip

Use `@sjsf/zod4-validator/mini` to work with `zod/mini`

:::

<Demo name="zod4" iconsPicker client:visible />

## Unrepresentable types

Types like `z.date()`, `z.bigint()`, `z.map()`, and `z.set()` have no JSON Schema equivalent. The adapter converts them to `{}` (unknown), rendering the default unknown field. You can wire custom field components via [UI schema](/form/ui-schema/#ui-components) to handle these types.
See the <OpenInSandbox client:visible example="Zod Unserializable" /> example for a working demo.

## Async validation

This validator supports async validation.

```typescript
import { adaptAsync } from "@sjsf/zod4-validator/classic";

const { schema, validator } = adaptAsync(zodSchema);
```
