---
title: Valibot
type: package
packageCodeName: "valibot"
sidebar:
  order: 2
---

import { externalValidatorPackage } from "meta";

import OpenInSandbox from "@/components/open-in-sandbox.svelte";

import { getPackageDependencies } from "@/shared";
import Npm from "@/components/npm.astro";
import Deps from "@/components/peer-dependencies.astro";
import Demo from "@/components/demo.svelte";

export const pkg = externalValidatorPackage("valibot");

You may want to use Valibot validator because:

- You can use Valibot schema as a source of truth for the form value type (`v.InferInput`).
- This is an easy way to add custom error messages

:::note

Schemas with unsupported types (`v.date()`, `v.bigint()`, etc.) are silently converted to `{}` (unknown). See the [supported features](https://github.com/fabian-hiller/valibot/blob/main/packages/to-json-schema/README.md#supported-features) for details.

:::

## Installation

<Npm pkg={getPackageDependencies(pkg)} />

<Deps pkg={pkg} />

## Example

<Demo name="valibot" iconsPicker client:visible />

## Unrepresentable types

Types like `v.date()`, `v.bigint()`, and `v.file()` have no JSON Schema equivalent. The adapter converts them to `{}` (unknown), rendering the default unknown field. You can wire custom field components via [UI schema](/form/ui-schema/#ui-components) to handle these types.
See the <OpenInSandbox client:visible example="Valibot Unserializable" /> example for a working demo.

## Async validation

This validator supports async validation.

```typescript
import { adaptAsync } from "@sjsf/valibot-validator";

const { schema, validator } = adaptAsync(valibotSchema);
```
