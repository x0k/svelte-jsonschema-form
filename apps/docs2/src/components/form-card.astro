---
import { Card } from "@astrojs/starlight/components";

export interface Props {
  style?: string;
}
---

<Card title="">
  <template shadowrootmode="open">
    {Astro.props.style && <style set:html={Astro.props.style} />}
    <slot />
  </template>
</Card>
