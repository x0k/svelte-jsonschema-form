---
import { Card } from "@astrojs/starlight/components";

import { ShadowHost } from "./shadow";

export interface Props {
  style?: string;
}
---

<Card title="">
  <ShadowHost style={Astro.props.style} client:only="svelte">
    <slot />
  </ShadowHost>
</Card>
