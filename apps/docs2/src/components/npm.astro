---
import { PKG_MANGERS } from "@/shared";
import {
  PackageManagers,
  type PackageManagersProps,
} from "starlight-package-managers";

export type Props = PackageManagersProps;
---

<PackageManagers pkgManagers={[...PKG_MANGERS]} {...Astro.props} />
