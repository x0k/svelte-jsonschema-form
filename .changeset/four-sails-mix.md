---
"@sjsf/form": minor
---

Add support for self-referential schemas

Enables rendering of recursive data structures like trees and linked lists
where a schema references itself via `$ref`.

Replaces <https://github.com/rjsf-team/react-jsonschema-form/pull/5118>
