---
"@sjsf/form": major
---

- Added a **required** `path` property to the `Config` type
- Updated the `JsonPaths` type to return a union of `Path`s
- Replaced the `name` prop with `path` in the `Field` component
