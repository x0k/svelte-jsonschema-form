---
"@sjsf/form": minor
---

- Add query `current` writable property to `Query`, replacing the read-only `result`.
- Add `initialValue` option to `createQuery`. When provided, the query `current` type excludes `undefined` automatically.
