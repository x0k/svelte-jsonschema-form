import type { Schema, UiSchemaRoot, ValidatorError } from '@sjsf/form';

export const schema: Schema = {};

export const uiSchema: UiSchemaRoot = {};

export const errors: ValidatorError<unknown>[] = [];
