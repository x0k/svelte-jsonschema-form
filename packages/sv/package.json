{
  "name": "@sjsf/sv",
  "version": "0.2.3",
  "description": "sv add-on for svelte-jsonschema-form",
  "keywords": [
    "sv-add",
    "svelte",
    "sveltekit"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/x0k/svelte-jsonschema-form.git",
    "directory": "packages/sv"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "default": "./dist/index.mjs"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "demo-create": "sv create demo --types ts --template minimal --no-add-ons --no-install",
    "demo-add": "pnpm build && sv add file:../ --cwd demo --no-git-check --no-install",
    "demo-add:ci": "pnpm build && sv add file:../=who:you --cwd demo --no-git-check --no-download-check --no-install",
    "build": "tsdown",
    "test": "vitest run",
    "check": "tsc",
    "lint": "eslint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check ."
  },
  "devDependencies": {
    "@sveltejs/sv-utils": "catalog:",
    "@types/node": "catalog:",
    "meta": "workspace:*",
    "sv": "^0.16.5",
    "tsdown": "^0.22.14",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "sv": "^0.16.0"
  }
}
